[package]
name = "rgis"
version.workspace = true
authors.workspace = true
edition.workspace = true

[lints]
workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[features]
# Enable eguidev automation instrumentation (native only).
eguidev = ["rgis-ui/eguidev"]

[dependencies]
bevy = { workspace = true }
bevy_jobs = { workspace = true }
geo = { workspace = true }
rgis-camera = { path = "../rgis-camera" }
rgis-crs = { path = "../rgis-crs" }
rgis-events = { path = "../rgis-events" }
rgis-file-loader = { path = "../rgis-file-loader" }
rgis-grid = { path = "../rgis-grid" }
rgis-layers = { path = "../rgis-layers" }
rgis-mouse = { path = "../rgis-mouse" }
rgis-primitives = { path = "../rgis-primitives" }
rgis-renderer = { path = "../rgis-renderer" }
rgis-settings = { path = "../rgis-settings" }
rgis-transform = { path = "../rgis-transform" }
rgis-ui = { path = "../rgis-ui" }
rgis-ui-messages = { path = "../rgis-ui-messages" }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rgis-cli = { path = "../rgis-cli" }

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = { workspace = true }
wasm-bindgen = { workspace = true }
bevy = { workspace = true, features = ["webgl2"] }
getrandom = { workspace = true }
# zstd-sys (via async-tiff -> geo-raster) compiles amd64 assembly for any
# non-Windows target; force no_asm so it builds for wasm32. Feature unification
# propagates this to the transitive copy.
zstd-sys = { version = "2.0.16", features = ["no_asm"] }
